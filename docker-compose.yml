version: '3.8'

services:
  vpn-bot:
    build: .
    container_name: vpn-checker-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./checked:/app/checked
      - ./logs:/app/logs
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
